import { HideCratesPipe } from './hide-crates.pipe';

describe('HideCratesPipe', () => {
  it('create an instance', () => {
    const pipe = new HideCratesPipe();
    expect(pipe).toBeTruthy();
  });
});
